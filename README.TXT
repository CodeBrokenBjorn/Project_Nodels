This project requires you to install every library to able to access and run this project.
1. First requirement is to install npm init -you
2. npm install express morgan nodemon mysql2 sequelize --save -> this will install all the required libraries...
3. Make sure that all the tools that are installed are correcly inputed in right files.
4.
5.If you get an error state missing Hire_Table that means u don't have database in datathingy
6.Make sure you name your database under what code is
